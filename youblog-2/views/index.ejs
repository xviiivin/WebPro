<!DOCTYPE html>
<html lang="en">

<%- include('header') -%>

    <body>
        <div>
            <!-- <h1>
            <%= title %>
        </h1>
        <ul>
            <% article.forEach(function(data, i, arr){ %>
                <li>
                    <%= data.title %>
                </li>
                <% }); %>
        </ul> -->
            <div class="container my-6">
                <form method="GET">
                    <div class="columns">
                        <div class="column is-11">
                            <div class="field">
                                <div class="control">
                                    <input class="input" type="text" name="search" placeholder="ค้นหาบทความ..." />
                                </div>
                            </div>
                        </div>
                        <div class="column is-1">
                            <input type="submit" class="button is-info is-fullwidth" value="ค้นหา" />
                        </div>
                    </div>
                </form>
                <!--  -->

                <div class="columns">
                    <% article.forEach(function(data, i, arr){ %>
                        <!-- <%= JSON.stringify(article) %> -->
                        <div class="column is-3">
                            <a href="#">
                                <div class="card" style="height: 100%">
                                    <div class="card-image">
                                        <figure class="image is-4by3">
                                            <img src="image/<%= data.feature_image %>" />
                                        </figure>
                                    </div>
                                    <div class="card-content">
                                        <div class="media">
                                            <div class="media-content">
                                                <p class="title is-4">
                                                    <%= data.title %>
                                                </p>
                                                <p class="subtitle is-6">by <%= data.author %>
                                                </p>
                                            </div>
                                        </div>
                                    </div>
                                </div>
                            </a>
                        </div>

                        <% }); %>


                </div>
            </div>
        </div>
    </body>

</html>